function getIconUrl(iconName: string): string {
	const baseUrl = `https://${S3_BUCKET}.s3.${S3_REGION}.amazonaws.com`;
	return `${baseUrl}/icons/${BUILD_VERSION}/${iconName}.svg`;
}

export default getIconUrl;
